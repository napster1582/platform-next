import React from 'react';

export const Icon = (): React.JSX.Element => (
    <span
        style={{
            fontWeight: 'bold',
            fontSize: '1.9rem',
        }}
    >
        Jinen CMS
    </span>
);
