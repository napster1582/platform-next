import { GlobalConfig } from 'payload/types';

const Footer: GlobalConfig = {
    slug: 'footer',
    fields: [
        {
            name: 'title',
            label: 'Título',
            type: 'text',
        },
    ],
};

export default Footer;
