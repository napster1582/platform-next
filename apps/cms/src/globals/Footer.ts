import { GlobalConfig } from 'payload/types';

const Footer: GlobalConfig = {
    slug: 'footer',
    fields: [
        {
            name: 'title',
            type: 'text',
        },
    ],
};

export default Footer;
