import { Block } from 'payload/types';

const AboutPage: Block = {
    slug: 'About',
    fields: [
        {
            name: 'prueba',
            label: 'prueba',
            type: 'text',
        },
    ],
};

export default AboutPage;
