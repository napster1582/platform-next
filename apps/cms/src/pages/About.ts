import { Block } from 'payload/types';

const AboutPage: Block = {
    slug: 'About',
    fields: [
        {
            name: 'prueba',
            type: 'text',
        },
    ],
};

export default AboutPage;
