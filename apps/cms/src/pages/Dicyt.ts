import { Block } from 'payload/types';

const DicytPage: Block = {
    slug: 'DICYT',
    fields: [
        {
            type: 'tabs',
            tabs: [],
        },
    ],
};

export default DicytPage;
