import { Template } from 'tinacms';

export const AboutTemplate: Template = {
    name: 'about',
    label: 'About',
    fields: [
        {
            name: 'age',
            label: 'Edad',
            type: 'number',
        },
    ],
};
